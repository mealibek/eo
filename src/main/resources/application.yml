# DataSource Configuration
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eo
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  # Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
